node_modules/
web/dist/

# secrets / local config
.env
.env.local
bot.config.json
accounts.json

# per-user generated data (keep .skill/ — it's the shareable skill pack)
projects/
requirements/

# os
.DS_Store
