# Bàn giao — 2026-08-01

Trạng thái để **phiên Claude Code mới** nối tiếp. Đọc file này trước.

## 1. Trạng thái upstream (nguồn chân lý)

`TranDuy13/agile-studio` **`main` = `159bce6`** — **0 PR mở, 0 issue mở**. Tất cả đã lên upstream:

- Hạ tầng: **Yarn 4** (packageManager + `.yarn/releases`, corepack), **CI** (GitHub Actions matrix
  3 OS × Node 22/24/26, `yarn test`), **docker-compose** (Dockerfile + compose.yaml).
- 9 feature cũ (#4,7,8,9,10,11,12,13,20): login/env-paths, account-UX, discord-bot, project-settings,
  killchild, **pluggable-storage (json/sqlite/postgres)**, project-management, session-usage, responsive.
- 4 nội bộ: add-account-loading, integration-status, usage-dialog, ui-polish.
- **docgen D1** (#36): tạo bộ tài liệu chuẩn quốc tế + cổng duyệt dàn ý.
- **docgen D2** (#38): viết nội dung, tiến độ, xuất `.docx`, routing hash. `DEV_LOCK_ECONOMY=false`
  (đã bỏ ép haiku). 5 bộ test (343 ca, 0 token) chạy trong CI.

Tổng: **18 PR merged** phiên trước, mọi issue liên quan closed.

## 2. ⚠ `personal/local-work` giờ đi SAU upstream

Nhánh tích hợp cá nhân này chứa **bản docgen giai đoạn phát triển** (`DEV_LOCK_ECONOMY=true`, bộ test
CŨ) + `docs/issues/` riêng tư. Upstream đã có bản docgen **đã ship + fix** (economy default off, test
sửa cross-platform). → **Việc mới (D3+) bắt đầu từ `origin/main`, KHÔNG từ local-work.** Nếu vẫn muốn
dùng local-work để dev, cân nhắc reset nó về `origin/main` rồi cắm lại `docs/issues/` (mục 4B).

## 3. Quy ước & bẫy đã học (áp cho phiên sau)

- **Remote đảo:** `origin` = upstream `TranDuy13` (chỉ qua PR), `fork` = `doducminh` (push thoải mái).
  Xác minh `git remote -v` trước mỗi push.
- **Được phép merge upstream bằng CLI** khi chủ repo đã duyệt: `gh pr merge <n> --repo TranDuy13/agile-studio --rebase`.
- **Quy trình fork-based PR** theo [theboysvn/engineering-standards](https://github.com/theboysvn/engineering-standards)
  (bản agent: [`docs/engineering-standards.md`](../engineering-standards.md)). Base `TranDuy13:main`,
  `Fixes #N`, request review `@TranDuy13`, một PR một việc, Conventional Commits.
- **`docs/issues/` KHÔNG lên upstream** — kiểm `git ls-tree` trước khi mở PR.
- **CI chạy `yarn test` thật trên 3 OS** → docgen tests phải cross-platform:
  - Đừng dùng path Windows kiểu `"Z:/..."` (POSIX coi là **tương đối** → rơi trong repo, sai `checked`).
    Dùng path tuyệt đối ngoài repo (vd `dests.DEST_DATA + "/..."`).
  - Test economy giả định trạng thái ON → phải bật tường minh qua API/`{on:true}`, vì bản ship default OFF.
  - `~/.agile-studio` local có thể **che lỗi** → chạy test với data-dir sạch để giống CI:
    `mv ~/.agile-studio ~/.agile-studio.bak; STORAGE_DRIVER=json yarn test; mv back`.
- **CI KHÔNG bắt lỗi runtime** (yarn test không boot server đủ cho mọi thứ) → tự
  **smoke-test `node server/index.js`** khi PR chạm server.
- **Rebase stacked PR:** `git rebase --onto origin/main <old-base-sha> <branch>` để chỉ replay commit
  riêng, bỏ commit đã merged. Với merge lớn (App.jsx bị rewrite): **net-diff apply** — checkout file
  mới/owned từ nhánh nguồn, lấy file web đã-merge từ local-work, merge tay file server (giữ thay đổi main).

## 4. Việc còn lại

### A. docgen D3–D8 (khối lớn — spec sẵn ở `docs/issues/19-docgen/`)
| | File spec | Nội dung |
|---|---|---|
| D3 | `D3-cai-dat-va-tien-ich.md` | `detect.py`/`install.py`/`styleprobe.py`: tự dò + cài Python·Word·LibreOffice·Vale, đọc mẫu .docx. **Mở khoá D4–D5.** |
| D4 | `D4-xuat-word-theo-mau.md` | Xuất Word theo mẫu doanh nghiệp (font/màu/bảng kiểm soát) |
| D5 | `D5-xuat-pdf-va-bao-ve.md` | PDF thường + PDF chống sao chép |
| D6 | `D6-cham-diem-theo-chuan.md` | Chấm điểm theo chuẩn, trọng số chỉnh được |
| D7 | `D7-bat-loi-tu-ngu.md` | Bắt lỗi thuật ngữ (class không tồn tại, secret, thuật ngữ lệch) |
| D8 | `D8-huong-dan-su-dung.md` | Hướng dẫn sử dụng (README, tiếng Anh) |

Hiện `.docx` chỉ chạy khi máy có **Python + python-docx** (Dockerfile có sẵn; máy trắng thì D3 mới lo).

### B. Dọn dẹp (tôi làm được nhanh)
- Xoá ~18 nhánh chết trên fork (đã rebase-merge vào main): `chore/adopt-yarn`, `ci/github-actions`,
  `chore/docker-compose`, `feat/*`, `fix/*` (trừ `personal/local-work`, `wip/local-01-15`).
- Quyết định số phận `personal/local-work` (reset về main + cắm lại docs/issues, hay giữ nguyên).

### C. Chuẩn hoá quy trình (Part 3 plan standards — chưa áp)
- 1 PR chore thêm `.github/PULL_REQUEST_TEMPLATE.md`, `.github/ISSUE_TEMPLATE/*.yml`,
  `CONTRIBUTING.md`, `.github/CODEOWNERS` (`* @TranDuy13`). Tôi làm được.
- **Branch protection** cho `main` (CI xanh + ≥1 review + squash) — **cần TranDuy13 (admin)** bật trong
  Settings → Branches. Agent không có quyền.

## 5. Đề xuất thứ tự phiên sau
B (dọn nhánh) → C (templates + CODEOWNERS PR; chủ repo bật branch protection) → A: **D3 trước**
(mở khoá D4/D5) → D6/D7 → D8 cuối. Làm từng feature một như docgen D1/D2.
